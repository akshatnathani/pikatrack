(function(){
  // GitHub contributions are now tracked via account profile sync.
  // Browser activity is tracked strictly for active tab session duration.
  console.log('PikaDex: GitHub active tab tracking active.');
})();
